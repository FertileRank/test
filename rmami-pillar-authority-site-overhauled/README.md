# RMA of Michigan Pillar Authority Site

Generated: 2026-06-07

This project upgrades the raw static RMA of Michigan site into a pillar-style fertility care resource with a responsive vanilla JavaScript mega menu, expanded homepage topic coverage, enhanced footer, Article and FAQ schema, and one shared CSS and JS asset.

## Included

- 51 HTML pages.
- `assets/styles.css` with shared minified CSS.
- `assets/site.js` with vanilla JavaScript for the mega menu and newsletter form state.
- `_headers` with static-host cache-control guidance for HTML and assets.
- `sitemap.xml`, `llms.txt`, `redirects.csv`, and `internal-linking-plan.json`.

## Performance

No CSS frameworks, JavaScript frameworks, page builders, external fonts, or icon libraries are used. Images retain explicit dimensions and native loading hints. Production hosts should apply the included cache headers or equivalent server configuration.
